# JavaScript

- 호이스팅
- 구조 분해 할당 (Destructing assignment)
- `const` vs `function`
- 화살표 함수
- `==` vs `===`
- 클로저
  - 클로저의 활용
- `new` 연산자
- '배열 처리 함수'
  - `map()`
  - `filter()`
  - `reduce()`
  - `map(), filter(), reduce()`
- 이벤트 버블링
- 자바스크립트가 비동기를 처리하는 방법










